# Build artifacts. A `go build` of one command writes its binary into the
# working directory, so every main package in the root module can drop its own
# name here; repolint's build-artifact rule holds the two lists together.
/bin/
/dist/
/stage/
/npm/.stage/
/tempora
/tempora.exe
/e2ebench
/e2ebench.exe
/context-retrieval
/compaction
/context-maintenance-e2e
/runtime-resume
/extension-protocol-gen
/tempora-plugin-example
/tempora-studio-host
/signpath-contract
/studio-delta
/covergate
/pricecheck
/repolint
/workgroupassay
/write_heartbeat
/catalog-detector
/tool-surface-cache

# Go test/build scratch
*.test
*.out
*.coverprofile
coverage.out
cpu.prof
mem.prof

# Wails desktop artifacts
/desktop/build/*
!/desktop/build/appicon.png
!/desktop/build/appicon.svg
/desktop/desktop
/desktop/frontend/dist/*
!/desktop/frontend/dist/.gitkeep
/desktop/frontend/sourcemaps/
/desktop/frontend/wailsjs/
/desktop/frontend/package.json.md5

# Environment / local config (tempora.example.toml is the shared template)
.env
.env.local
*.local
/tempora.toml
/desktop/.env
/desktop/tempora.toml

# Node / frontend dependencies and caches
node_modules/
.pnpm-store/
.npm/

# Project-local Tempora state; keep the checked-in review command.
/.tempora/*
!/.tempora/commands/
!/.tempora/commands/review.md

# Stray Tempora state written under a package dir by a test whose RealPath
# resolves to CWD (e.g. remote bootstrap EnsureServe). Never commit these.
internal/**/.tempora/
desktop/**/.tempora/

# CodeGraph per-project index (rebuilt locally; never committed)
.codegraph/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/

benchmarks/context-maintenance-e2e/run/

# Local scratch
temp/

# Official theme art intermediate outputs / caches
scripts/official-theme-art/out/
scripts/official-theme-art/__pycache__/

# Committed pointer at TEMPORA.md; overrides a global CLAUDE.md ignore rule.
!/CLAUDE.md

# Go build output left in the tree by `go build ./tools/...`. The ~ form is
# what a linker leaves behind when it cannot replace a binary that is still
# running, which on Windows is every rebuild while Studio is open.
*.exe
*.exe~
