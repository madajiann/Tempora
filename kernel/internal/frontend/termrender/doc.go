// Package termrender draws Tempora output for a terminal: the colour theme
// and its profile-aware styling helpers, Markdown with inline math, tool
// cards, highlighted diffs, and clipboard writes. It holds no session state,
// so every terminal frontend renders the same bytes for the same input.
package termrender
