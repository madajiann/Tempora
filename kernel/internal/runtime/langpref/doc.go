// Package langpref carries which language a turn answers and reasons in: the
// preferences a host puts on the context, the transient blocks that tell the
// model, and inference from the user's own text when nothing was configured.
package langpref
