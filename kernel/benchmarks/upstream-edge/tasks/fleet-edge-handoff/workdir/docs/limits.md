# Limits, historically

The retry budget started at 3 while the v1 client was in use. That number was
revised during the v2 migration; `limits/` holds the value the runtime reads
today, and this document is not updated when it changes.
