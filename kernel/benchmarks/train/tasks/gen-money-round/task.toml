prompt = """In the finmath library, every monetary amount must be rounded to the nearest cent using round-half-even (banker's rounding): an amount exactly halfway between two cents goes to the even cent (e.g. 0.125 -> 0.12, 0.135 -> 0.14, 1.225 -> 1.22, 1.235 -> 1.24, 2.675 -> 2.68). The two modules currently round inconsistently: for several exact half-cent inputs, display.format_amount and ledger.round_to_cents disagree with each other and with round-half-even. Fix both modules so they agree with each other and with round-half-even on every input. Keep the public signatures and return types unchanged (format_amount -> str, round_to_cents -> float). The contract tests under tests/ document the expected behaviour; check your fix with `python3 -m unittest discover -s tests`."""
class = "multi-file-bugfix"
timeout_sec = 300
